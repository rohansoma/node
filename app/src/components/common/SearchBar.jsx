import { Input } from "@/components/ui/input";

export default function SearchBar() {
    return (
        <Input
            placeholder="Search..."
            className="w-64 bg-white"
        />
    );
}
